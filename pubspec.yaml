name: nolimit
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # TOAST
  fluttertoast: ^8.2.8

  # IMAGE
  cached_network_image: ^3.3.1

  # BASE
  intl: ^0.19.0
  google_fonts: ^6.2.1
  html: ^0.15.4
  shared_preferences: ^2.3.2
  flutter_widget_from_html: ^0.15.2

  # UI
  sliding_up_panel: ^2.0.0+1
  dotted_line: ^3.2.2
  lottie: ^3.1.2
  flutter_rating_bar: ^4.0.1
  flutter_staggered_animations: ^1.1.1
  bottom_picker: ^2.8.0
  country_code_picker: ^3.0.0
  flutter_native_contact_picker: ^0.0.7
  dotted_border: ^2.1.0
  timeline_tile: ^2.0.0
  flutter_vector_icons: ^2.0.0
  permission_handler:

  #OTP TEXTFIELD
#  otp_text_field: ^1.1.3
  pinput: ^5.0.0
#  flutter_otp_text_field: ^1.1.3+2

  # MAP
  geolocator: ^10.0.1
#  geolocator: ^12.0.0
  geocoding: ^3.0.0
  google_maps_flutter: ^2.9.0
  flutter_polyline_points: ^2.1.0
  google_maps_place_picker_mb: ^3.1.2

  # IMAGES
  image_picker: ^1.1.2
  file_picker: ^8.0.6

  # CONNECTIVITY
  package_info_plus: ^8.0.2

  # CONNECTIVITY
  http: ^1.2.2
  connectivity_plus: ^6.1.0
  url_launcher: ^6.3.0
  internet_file: ^1.2.0

  #FIREBASE
  firebase_crashlytics: ^4.1.0
  firebase_core: ^3.4.0
  firebase_auth: ^5.2.0
  firebase_storage: ^12.1.1
  firebase_database: ^11.0.3
  paginate_firestore: ^1.0.3+1
#  paginate_firestore:
#    path: ./lib/my_packages/paginate_firestore-master
#  flutter_paystack:
#    path: ./lib/my_packages/flutter_paystack-1.0.7
  cloud_firestore: ^5.3.0

  # STATE MANAGEMENT
  mobx: ^2.3.3+2
  flutter_mobx: ^2.2.1+1

  # Payment gateway
  #razorpay_flutter: ^1.3.7
  flutter_stripe: ^11.0.0
  #flutter_paystack:
  #  git:
  #    url: https://github.com/appmeetmighty/flutter_paystack.git
#    path: ./lib/package/flutter_paystack-master
#  flutter_paystack: ^1.0.7
  #flutter_braintree: ^4.0.0
  #flutterwave_standard: ^1.0.8
  flutter_paytabs_bridge: ^2.6.17
#  mercado_pago_mobile_checkout: ^0.2.0-dev.3
  my_fatoorah: ^3.3.0
  flutter_inappwebview: ^6.1.5
  #paytm: ^3.0.1


  # PUSH NOTIFICATION
  onesignal_flutter: ^5.2.4

  # MQTT
#  mqtt_client: ^10.2.0

  # Social Login
  google_sign_in: ^6.2.1
  the_apple_sign_in: ^1.1.1

  # DOWNLOAD INVOICE PDF
  pdf: ^3.11.1
  pdfx: ^2.6.0
  path_provider: ^2.1.4
  open_file: ^3.5.3
  printing: ^5.13.1

  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.11
  mobx_codegen: ^2.6.1
  #flutter packages pub run build_runner build --delete-conflicting-outputs

dependency_overrides:
  package_info_plus: ^4.1.0
  intl: ^0.18.0
  http: ^0.13.5
  cloud_firestore: ^5.3.0
  firebase_core: ^3.4.0
#  package_info_plus: ^4.1.0
#  web: ^1.0.0
#  geolocator: ^10.0.1



flutter:
  uses-material-design: true

  assets:
    - images/
    - images/flag/
    - images/icons/
